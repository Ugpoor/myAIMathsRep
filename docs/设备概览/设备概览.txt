”设备概览“是一个仪表盘视图，不是一个独立的功能，因此只能由首页仪表卡片中的“设备在线”卡片进入，进入后是一个仪表视图，container包括：设备接通量，接通和未接通数量饼图、异常事件仪表卡片（4列2行，依次为：意外断线、考内切屏、疑似抄袭、频繁切题、答题草率、驻留过长、未交卷、白卷），记录这些异常事件的总次数。
SubmenuTabs
筛选：在明细视图下，根据列值筛选，数字列值提供阈值筛选（最大、最小），总览视图下，不显示此标签。
总览：明细视图下显示此标签，进入总览视图，如上所述。
明细：总览视图下显示此标签，进入明细视图，提供所有设备的接入状态记录。该明细视图的container中，各条目字段有：设备号、学号、姓名、异常事件8个列（分别记录次数）。
